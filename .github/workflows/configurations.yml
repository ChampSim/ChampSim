name: Runtime configurations

on:
  push:
    branches: 
      - master
      - develop
    paths-ignore:
      - '.gitignore'
      - 'docs/**'
      - '*.md'
      - '.github/workflows/*.yml'
      # re-include current file to not be excluded
      - '!.github/workflows/configurations.yml'

  pull_request:
    branches:
      - master
      - develop
    paths-ignore:
      - '.gitignore'
      - 'docs/**'
      - '*.md'
      - '.github/workflows/*.yml'
      # re-include current file to not be excluded
      - '!.github/workflows/configurations.yml'

jobs:
  matrix-generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      
      - id: set-matrix
        name: Parse output
        run: python3 -c 'import os, json; print("matrix="+json.dumps(os.listdir("test/config")))' >> $GITHUB_OUTPUT

    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}

  check-configurations:
    runs-on: ubuntu-latest
    needs: matrix-generate
    strategy:
      matrix:
        file: ${{ fromJSON(needs.matrix-generate.outputs.matrix) }}

    steps:
    - uses: actions/checkout@v7
      with:
        submodules: 'true'

    - name: Set up
      run: |
        vcpkg/bootstrap-vcpkg.sh
        vcpkg/vcpkg install

    - name: Make
      run: make

    - name: Validate configuration
      run: ./bin/champsim --config test/config/${{ matrix.file }} --dump -w0 -i1 -- /dev/null 2>&1 | head -100

